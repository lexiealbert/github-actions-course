name: 05-exercise

on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo statement
        run: |
          echo "Running whenever a PR is opened or synchronized AND base branch is main"
          
